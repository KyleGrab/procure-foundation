"""
Background job definitions. FastAPI BackgroundTasks + job-status table through Phase 2 (see ADR-002); Celery app and task modules introduced in Phase 3 when import/matching volume justifies the operational cost.
"""
